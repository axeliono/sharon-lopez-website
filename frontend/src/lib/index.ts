// place files you want to import through the `$lib` alias in this folder.
import { darkModeStore } from './stores'; 
import { Navbar } from './Navbar.svelte'
import './Resource.svelte'

export default{
    darkModeStore
}